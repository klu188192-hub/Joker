---
date: 2026-05-04
type: daily-log
maintained_by: TT
tags: [daily, timeline]
---

# 2026-05-04 工作时间线 (TT 视角)

> 按时间倒序. 最新事件在顶部. 配合 `工作总览_index.md` 看全局.

## ⏱ 时间线 (北京时间, UTC+8)

### 16:43 跟单 P1.5 完成 (全面实时 copy)
- 修 close_position/partial_close 的 `_pick_filling(symbol)` → `_pick_filling(p.symbol)` (我前一次 replace_all 留下的 bug)
- 修 `_master_poll` 重复推送 (提前更新 `_prev_snap`, send 失败不再重复 diff)
- 加 mt5_wrapper.py: `pending_orders_snapshot()` / `place_pending` / `cancel_pending` / `modify_pending` / `diff_pending_snapshots`
- 扩 protocol/messages.py: TradeEvent + CopyOrder 加 `order_type` / `price` / `slave_ticket_target` 字段
- 扩 agent.py: master_poll 同时 diff 持仓 + 挂单, _exec_copy 处理 PEND_PLACE / PEND_CANCEL / PEND_MODIFY
- 扩 coordinator/server.py: 路由新 action, position_map 共用挂单映射, 在 OPEN/PEND_PLACE 都登记
- sync 4 文件 + restart coord/agents, 全 ONLINE, 双账户 0 持仓
- 已企微 push 提示用户手测

### 03:55 用户要求建工作总览
- 用户原话: "目前多任务进程会让我决策出错, 我需要看到总工作清单和工作进度"
- TT 已建: `Projects/工作总览_index.md` (本文上层) + 本文 (每日 log)
- 后续每完成一项立即更新 index + 在此 append 时间戳事件

### 03:50 trade_recap.py 草稿完成
- 文件: `/Users/joker/tt_modules/trade_recap.py` (Mac 本地, 未上传 KK)
- 功能: today_summary / recent_trades / open_positions / ds_decisions_today / all_accounts_overview
- 数据源: monitor_57171.db / ds57170/trades.db / starbridge_monitor.db (全是 KK 上现成的)
- 状态: 待 Joker 确认协议后部署 (避免动 wecom_bridge.py 内核)

### 03:40 TT-Joker 协调发出
- 提议: TT 只写 `tt_modules/` 子模块, 不动 wecom_bridge.py
- 三路通知: Obsidian md / Joker plan file 末尾 / 企微 push 给用户
- 文件: `Projects/TT-Joker-协调_2026-05-04.md`
- 等 Joker 30 min 内 (04:15 前) 回复

### 03:35 备忘录功能下架, 改用企微
- 用户原话: "备忘录功能下架, 全面使用企微功能代替"
- 已停: TT 指令 monitor + claude-task-sync + morning-status 三个 LaunchAgent
- 已建: `~/.claude/scripts/tt_push.sh` — TT 通过 ssh 调 KK 的 wecom-bridge /push 端点
- 测试通过: 已发 2 条测试消息到企微 LuKang (errcode=0)

### 03:31 wecom-bridge 重启 + 加进 watchdog
- wecom-bridge 死了 23 分钟没人发现 (03:08:55 最后日志)
- TT 重启 PID 7540, 验证 healthz OK
- 加进 watchdog (每 3 min 自检 + 自动重启)

### 03:30 浏览企微聊天记录提取用户抱怨
- 7 条核心抱怨: 复盘假数据 / FVG OB 标注不准 / 文字要在图里 / 跨会话调用 / etc.
- 详见 `工作总览_index.md` 的 "P5 Joker 企微图文优化" 任务上下文

### 03:14 全部监控健康验证
- 6 个 KK 进程全 ALIVE: DS controller / 57171 monitor / SB monitor / coord / agent_57170 / agent_57171
- Watchdog 每 3 min 自愈

### 03:08 跟单系统 P1 完整完成
- 1400 行代码 (协议 + coord + agent + reconciler + web UI + mock test + docs)
- 跨设备 mock test 全通过 (Mac → KK Tailscale)
- 4 个 git commit
- 详见 `Projects/MT5跟单系统_进度_2026-05-04.md`

### 02:55 Watchdog 部署 (KKWatchdog scheduled task)
- 每 3 min 检查 6 个关键 monitor PID
- 死了自动重启
- 日志: `kk:C:\Tools\watchdog.log`

### 02:30 跟单 mock smoke test 通过
- master 99001 → slave 99002 (fixed ratio 0.1)
- vol 0.10 → 0.01 缩放正确
- position_map 持久化 ✓
- CLOSE 时 target_ticket 来自 mapping ✓

### 01:50 跟单系统 P1.1 代码完成
- protocol/messages.py + coordinator/{server,db}.py + agent/{agent,mt5_wrapper}.py + web/index.html

### 01:00 跟单需求确认
- 用户: "我可以使用它链接任意 MT5 账户, 1 对多 / 多对 1, 跨设备, balance 比例, 所有改单实时 copy"
- TT 给 P1-P5 路径 + 选 P1 今晚开始

### 00:30 用户授权后台开发跟单
- 用户原话: "今晚开始, 直接开干, 但你不可以耽误明天的账户监控和交易管理"

### 00:00 (前一日 23:50) iCloud Vault 同步通了
- 测试文件 → Mac → iCloud → iPhone 全链路
- LaunchAgent 15 min 同步频率

### (昨日 23:30) GitHub 备份链路通了
- klu188192-hub/Joker private repo
- LaunchAgent 每天 20:30 自动 git push

### (昨日 23:25) Scalper20_M1 部署 EBC-mt5/57171
- 用户挂 EA 到 BTCUSD/XAUUSD.s/SPXUSD.s 三个 chart
- monitor_57171.py 启动抓单

### (昨日 23:00) DS 主导 57170 controller 启动
- DeepSeek-Reasoner + 60s 轮询 + 风控护栏
- 等开盘后第一笔决策

### (昨日 21:00) AI 量化架构方案 (DS+TT 互验)
- 选 B 方案 (EA 信号 + LLM 过滤)
- 月成本预估 $12-15

## 📊 今日已完成 vs 待办计数

- ✅ 已完成: 14 项 (跟单 P1 / 监控 watchdog / 备忘录下架 / 企微通道 / Obsidian sync / GitHub backup / 协调提议 / etc.)
- 🚧 进行中: 4 项 (TT-Joker 协调 / tt_modules 草稿 / Joker 企微图文 / 跟单实盘 smoke 等待)
- ⏸️ 待启动: 5 项 (AI D1 / A/B 测试 / VPS 部署 / GitHub copier / 用户决策项)

## 🎯 接下来 1 小时计划 (04:00 → 05:00)

- [ ] 04:00 等 Joker 协调回复 (写 trade_recap.py 测试用例)
- [ ] 04:15 没收到拒绝 → 静默写 smc_detector.py + query_real_data.py 草稿
- [ ] 04:45 看监控状态 (有无新事件)
- [ ] 05:00 写 05:30 切换前的最后状态报告

## 🚦 5:30 切换 → 监控模式

到点后:
- 跟单后台暂停手动迭代 (watchdog 维持自愈)
- TT 主任务转为: 监控 3 账户开盘后第一批数据
- 06:00 自动 LaunchAgent (现已改为企微 push 替代备忘录)

---

**注**: 此文每完成一项立即 append 时间戳条目. 用户可以直接看顶部最新事件就知道 TT 在干什么.

---

## 🤖 CC (Joker Mac) 工作记录 — 2026-05-04 深夜

> CC 在用户下班前完成，按完成顺序正序排列。

### 21:00± 上下文自动备份系统部署 ✅
- `~/.claude/scripts/context_guard.py` — PostToolUse hook, 每20次工具调用注入 additionalContext 提醒
- `~/.claude/scripts/precompact_hook.sh` — PreCompact hook, 压缩前强制提醒写备份
- `~/.claude/scripts/stop_hook.sh` — Stop hook, 会话结束写时间戳
- `~/.claude/settings.json` 加三层 hook; `CLAUDE.md` 加会话启动强制读备份指令

### 21:30± 企微图表视觉重写（4轮 patch） ✅
- patch1: 线条式 SMC 标注（原矩形→双边框线）、bar_idx 定位起点、标准绿(#089981)/红(#f23645)
- patch2: `ax.set_xlim(-0.5, x_right+10)` 修复预判箭头和标签被裁剪
- patch3: 隐藏全部坐标轴；关键价格浮标（zone 上下沿 + 流动性线内嵌价格数字）；K线50根变细
- patch4: BOS/CHoCH 市场结构检测 + 渲染（蓝实线/琥珀虚线，白底框标签）
- WeComBridge 计划任务重启，新进程 PID 20568，所有 SMC 发布走新样式

### 22:00± smc_detector.py 重写 — 新增 BOS/CHoCH ✅
- `find_pivots(df, lb=3)` — 对称窗口寻轴高轴低
- `find_bos_choch(df)` — 按时间序扫描，每次突破更新 trend 状态，BOS=延续/CHoCH=反转
- `detect_all()` 新增 `'structure'` 字段；`to_render_annotations()` 传 `bar_idx`/`break_idx`/`color`/`direction`
- 自检验证：XAU H4 确认熊市结构(4×BOS↓ + 2×CHoCH)，XAU H1 捕捉到多次趋势转换

### 22:30± DS 57170 controller 两个 bug 修复 ✅

**Bug 1 — no_api_key（已由 TT 写入 key 解决）**
- 根因：controller 02:04 UTC 启动时 key 文件未就绪；12:00 UTC 后 key 存在，API 调用恢复正常

**Bug 2 — retcode=10030 INVALID_FILL（CC 今晚修复）**
- 根因：`execute_decision` 硬编码 `ORDER_FILLING_IOC`，EBC broker 仅支持 FOK
- 修复：新增 `get_filling_type(symbol)` 动态读 `symbol_info.filling_mode` bitmask 决定 fill type
- DS controller PID 29136 已杀，新进程 PID 28740/30068 带修复运行

### 22:40 自检小结
| 组件 | 状态 | 说明 |
|---|---|---|
| WeComBridge (PID 20568) | ✅ 运行 | 新代码，SMC 新样式生效 |
| DS 57170 controller (PID 28740) | ✅ 运行 | fill type 动态检测，等下次交易窗口验证 |
| 上下文备份 | ✅ 活跃 | 3层 hook 全部生效 |
| BOS/CHoCH 检测 | ✅ 验证 | 干测通过，下次发图自动出现 |

**明日待验证：**
- DS 04:00-06:00 UTC 窗口，若出现 OPEN BUY/SELL，retcode 应为 10009(DONE)
- 企微推图 BOS/CHoCH 标注是否符合用户预期

---
date: 2026-05-04
type: daily-work
author: TT (Claude Code)
tags: [daily, factor-factory, p2-lgbm, ldj-factors]
---

# 2026-05-04 每日工作记录

> 整理时间：2026-05-04 下班后（CC 自动生成）

---

## ✅ 今日完成

### P1.3 因子筛选（上午完成）
- 最终产出 **19 个稳健因子** → `factor_pool_final.csv`
- avg pairwise corr = 0.225（达标 <0.4）
- 最优单因子 val_IR = 0.727（r_neg_log_return_50）
- 注：未达 30-50 因子目标，单品种限制，已记录

### P2.1 LightGBM Walk-Forward（今晚完成）
- **实现**：`models/lgbm_walkforward.py` + `scripts/run_p2_lgbm.py`
- **运行**：40 折 Purged Walk-Forward (2Y train / 3M test / 1M step / 192 bar purge)
- **结果**：

| 区间 | IR | 是否达标 |
|---|---|---|
| 全期 | -0.136 | ❌ |
| 验证(2024-25) | -0.112 | ❌（目标 >0.873）|
| 测试(2026-) | -0.755 | ❌ |

- **Feature importance top-3**：r_neg_log_return_50、r_williams_r_50、v_range_ratio_20
- **根因分析**（CC 诊断）：
  1. M15 EBC 数据 63% 为填充空 bar（volume=0，OHLC 全等）→ 特征大量为常数 0 → 模型无法学习
  2. 多数折仅建 1-2 棵树（early stopping 过激），信号极弱
  3. 预测 192 bar (48h) 是较长视野，binary 分类噪声大

### 李德江实盘盈利因子移植
- 读取 `Trades/8010234-StarBridge/李德江盈利因子报告_2026-05-04.md`
- **识别 5 类因子**（A1+A3 信号的量化表达）：

| 因子名 | 来源信号 | 描述 |
|---|---|---|
| `ldj_vwap_deviation` | A1 VWAP偏离 | (close-VWAP)/std，量少时用MA替代 |
| `ldj_low_vol_reversion` | A1 缩量均值回归 | -zscore(close) × (1-vol_rank) |
| `ldj_pinbar_bull` | A3 Pinbar | 下影线-上影线，看多针形正值 |
| `ldj_pinbar_with_vol` | A3 带量针形 | pinbar × volume_zscore |
| `ldj_bar_dir_align` | bar_dir特征 | 多空K线比例 −1~+1 |
| `ldj_session_favor` | UTC时段权重 | 好时段(01/04/10/14/19)+，差时段(07/09)- |

- **已加入** `factor_templates.py` TEMPLATE_DEFS
- **约束**：M15 EBC 数据 volume>0 仅 1%，量价因子在此数据集基本无效
  → 需 Dukascopy M15 数据才能发挥作用（有完整成交量）

---

## 🔴 明日必做（明天来检查时直接开始）

### 优先级 1：P2.1 数据质量修复
- **方案 A（快，2h）**：换 H1 数据重跑 P2.1
  - H1 只有 16% 填充率，量价信息更完整
  - `python scripts/run_p2_lgbm.py --tf H1 --fwd 5`
- **方案 B（根本解，4h）**：从 KK 同步 Dukascopy M15 → 用完整数据
  - `scp "kk:D:/factor_factory/data/XAUUSD.s_M15_duka.csv" /Users/joker/factor_factory/data/`
  - 重新生成因子矩阵（含 ldj 因子）
  - 重跑 P1.3 筛选 → 重跑 P2.1

### 优先级 2：P2.1 模型参数优化（基于当前结果）
- 改用 `regression` 目标（而非 binary），预测实际收益率
- 降低 early stopping = 200（当前 50 过激）
- 尝试更短预测视野 fwd=5（1.25h）

### 优先级 3：ldj 因子筛选
- 重新生成因子矩阵（加入 ldj 22 个新因子 → 总 772 个）
- 命令：删 `data/factors_M15_v1.csv` 后重跑 `scripts/run_p1_screening.py`

---

## 文件清单

| 文件 | 状态 | 备注 |
|---|---|---|
| `factors/factor_templates.py` | ✅ 更新 | 新增 6 类 22 个 ldj 因子 |
| `models/lgbm_walkforward.py` | ✅ 新建 | P2.1 核心模块 |
| `scripts/run_p2_lgbm.py` | ✅ 新建 | P2.1 runner |
| `reports/factor_pool_final.csv` | ✅ 存在 | 19 个稳健因子 |
| `reports/signal_nl_M15_fwd192.csv` | ✅ 新建 | P2.1 信号输出（未达标）|
| `reports/p2_feature_importance.csv` | ✅ 新建 | 特征重要性 |

---

## 技术债

- lightgbm dylib rpath 修改（手动 `install_name_tool`）→ 重装 Python 后失效
  - 正确解：安装 homebrew → `brew install libomp`
- matplotlib 未安装 → 图表跳过（正在后台安装）

---

## 李德江账户要点（复用备忘）

| 盈利因子 | 组 | mean PnL | WR | 启示 |
|---|---|---|---|---|
| vol_bucket 1-2 | 高手数 | $+537 | 88.9% | 大手=高确信信号 |
| dur_bucket ≤1m | 极短持仓 | $+405 | 94.7% | 快速TP=趋势明确 |
| has_tp_marker True | TP命中 | $+336 | 100% | 挂TP是关键 |
| hour_utc 01 | 01:00 UTC | $+448 | 66.7% | 亚洲开盘甜区 |
| 亏损：has_sl_marker True | SL触发 | -$438 | 0% | 不挂硬SL策略核心 |
